import userEndpoints from './user';

const endpoints = {
	users: userEndpoints,
};

// there's an object called endpoints, which gets exported from endpoints
// api.router.users.getUsers
export default endpoints;

// next let's make userEndpoints
